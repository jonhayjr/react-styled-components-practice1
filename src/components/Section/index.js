import { SectionElement } from "./SectionElements";


const Section = ({children}) => {
    return (
        <SectionElement>
            {children}
        </SectionElement>
    )
}

export default Section;